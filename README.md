These are scripts to provision the [corbt](https://corbt.com) server.

The base system is running CoreOS, and most services are running behind the dockerized [nginx-proxy](https://github.com/corbt/nginx-proxy).